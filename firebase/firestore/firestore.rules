rules_version = '2';

// TODO: WARNING - INSECURE DEVELOPMENT RULES
// They MUST be replaced with secure rules before launching to production.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
