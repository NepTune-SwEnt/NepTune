package com.neptune.neptune.ui.profile

/**
 * Represents the type of profile view: self: for viewing the currently signed-in user's profile, or
 * other user: for viewing another user's profile
 */
enum class ProfileViewType {
  SELF,
  OTHER_USER
}
